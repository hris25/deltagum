{
  "buildCommand": "pnpm dlx prisma generate && next build",
  "installCommand": "pnpm install && pnpm dlx prisma generate",
  "env": {
    "PRISMA_GENERATE_DATAPROXY": "true"
  },
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  }
}
